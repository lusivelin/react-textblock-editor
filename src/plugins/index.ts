import type { EditorPlugin } from "../core/plugin-registry";

// Default plugin set. Extend this array to add new toolbar functionality
// without modifying any existing component.
export const DEFAULT_PLUGINS: EditorPlugin[] = [];
