# Publishing

## Prepare

1. Run `pnpm install`.
2. Run `pnpm build:lib`.
3. Check `dist/` contents.

## Publish

```bash
pnpm publish --access public
```

Adjust access level to match your npm scope policy.
